#This script reads a CSV file and prints each row.

import csv

def read_csv(file):
    with open(file, 'r') as f:
        reader = csv.reader(f)
        for row in reader:
            print(row)

read_csv('file.csv')
